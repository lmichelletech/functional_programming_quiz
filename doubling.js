//map practice


//doubling values in an array

const array = [6, 7, 8]

//give name arr for ech instance of this array
var newArrayMap = array.map(function(arr){

    //alaways need a return
    return arr * 2
})

console.log(array)
console.log(newArrayMap)